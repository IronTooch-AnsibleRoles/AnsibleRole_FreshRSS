#
# Note: After making changes to this file you need to run the `rebuild` or `update`
# command for them to be applied.

url: https://{{ freshrss_external_domain }}
http_port: 80
https_port: 443
ssl_certificate_path: {{ freshrss_certificate_path }}
ssl_key_path: {{ freshrss_key_path }}}

yaml_list:
{% for internal_yaml_item in freshrss_list_data %}
  - {{ internal_yaml_item }}
{% endfor %}
